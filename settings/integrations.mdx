---
title: "Integrations settings"
description: "Local API Key, browser plugin, and External Search"
---

## Entry

**Settings → Integrations**

## Local API Key

Authenticates the local HTTP API for plugins on this Mac. Please:

- Rotate periodically (when the UI offers it)  
- Never commit it to public git  
- Use only on trusted local machines  

## Browser plugin

| Item | Description |
|------|-------------|
| Enable | Starts the plugin local service |
| Port / Token | Must match what the extension sends |
| Prerequisite | **Starcat must be running** |

Request shape: `http://127.0.0.1:{port}/plugin/v1/...` with `Authorization: Bearer <token>`.

See [Chrome](/browser-plugin/chrome), [Features](/browser-plugin/features), and [Safari](/browser-plugin/safari).

## External Search

Configure web search and AI web-context providers. Steps are in [External search](/search/external-search).

The nav page [External search settings](/settings/external-search) points here.

## Desktop launchers

Alfred, uTools, and Raycast companions live in separate GitHub repos. Setup: [Desktop launchers](/integrations/desktop-launchers).

## Other

May include CodeFlow / CodebaseMemory output directories and similar items. Follow the UI; do not treat unreleased capabilities as shipped.
