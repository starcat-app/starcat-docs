---
title: "智能集合"
description: "Smart Collections —— 自定义动态过滤规则"
---

## 什么是智能集合？

智能集合（Smart Collections）是基于规则动态分组的仓库集合。设定过滤条件后，符合条件的仓库自动归入该集合。

<Note>Pro 用户可以创建多个智能集合，免费版限制 1 个。</Note>

## 创建智能集合

<Steps>
  <Step title="新建集合">
    在侧边栏 Smart Collections 区域点击「+」。
  </Step>
  <Step title="设置规则">
    定义过滤条件。支持多条件组合（AND / OR）：
    - 语言
    - 标签
    - Star 数范围
    - 状态
    - 更新时间
    - 知识库状态
  </Step>
  <Step title="命名">
    给集合一个有意义的名称，如「AI 项目」「关注中的 Go 库」。
  </Step>
</Steps>

## 规则编辑器

智能集合提供可视化规则编辑器：

- **添加条件** — 点击「+」添加新条件
- **条件类型** — 从下拉菜单选择过滤维度
- **运算符** — 等于、包含、大于、小于等
- **值** — 输入或选择目标值
- **逻辑关系** — AND（全部满足）/ OR（任意满足）

## 使用智能集合

创建后，智能集合出现在侧边栏中：

- 点击集合名查看当前匹配的仓库
- 集合自动更新，无需手动管理
- 右键可以编辑规则、重命名或删除
